import 'package:flutter/material.dart';

// Light theme colors
class AppColors {
  static const primary = Color(0xFF1E2746);
  static const cursor = Color(0xFF429FF6);
  static const accent = Color(0xFFE94E77);
  static const background = Color(0xFFEDF1F3);
  static const bgSecondary = Color(0xFFFFFFFF);
  static const textPrimary = Color(0xFF333333);
  static const iconColor = Colors.black87; // for AppBar icons & other icons
  static const labelColor = Colors.blue;
}
